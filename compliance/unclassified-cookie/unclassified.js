// Unclassified cookie - unclassified-cookie
//
// Proprietary name that matches no known cookie database, so the scanner finds
// it on load but cannot auto-categorize it — it lands in the Unclassified category.
(function() {
  document.cookie = 'unclassified_test=uc_' + Math.random().toString(36).substr(2,24) + '; max-age=' + (365*86400) + '; path=/; SameSite=Lax';
})();
